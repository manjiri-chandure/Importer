<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.importer.importer.repository.LogRepository" >

    <insert id = "addLog" parameterType="com.importer.importer.dto.LogDto" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into log(student_name, gender, age, status_code, response_message, timestamp)
        values (#{ld.fullName}, #{ld.gender}, #{ld.age}, #{ld.statusCode}, #{ld.ResponseMessage}, #{ld.timeStamp})
    </insert>

</mapper>

