<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.importer.importer.repository.LogRepository" >

    <insert id = "addLog" parameterType="map" useGeneratedKeys="true">
        insert into log(student_name, gender, age, status_code, response_message)
        values (#{scd.fullName}, #{scd.gender}, #{scd.age}, #{sc}, #{rm})
    </insert>

</mapper>

